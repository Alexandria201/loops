for (let forCount = 1; forCount <= 8; forCount++) {
    console.log(forCount);
}

let doCount = 1;
do {
    console.log(doCount)
    doCount++
} while (doCount <= 8)


let whileCount = 1;
while (whileCount <= 8) {
    console.log(whileCount);
    whileCount++;
}